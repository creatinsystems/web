// Tailwind CSS v4 — primary configuration lives in src/app/globals.css via @theme.
// This file is present for tooling compatibility and plugin registration.
import type { Config } from 'tailwindcss';

const config: Config = {
  // Intentionally left empty — Tailwind v4 configuration lives in CSS
  // (e.g. via @config and @theme in src/app/globals.css). This file exists
  // primarily for IDE tooling and potential plugin registration.
};
export default config;
